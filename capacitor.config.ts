import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.toolhopp.app',
  appName: 'ToolHopp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
